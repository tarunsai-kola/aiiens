const mongoose = require('mongoose'); async function run() { await mongoose.connect('mongodb://localhost:27017/hospital_management'); const db = mongoose.connection.db; const result = await db.collection('users').updateMany({}, { '$set': { password: '720446@Sai' } }); console.log('Updated passwords for ' + result.modifiedCount + ' users.'); process.exit(0); } run().catch(console.error);
