const Joi = require('joi');

// TODO: Define validation schemas for ward
const createWardSchema = Joi.object({
  // Add validation rules here
});

const updateWardSchema = Joi.object({
  // Add validation rules here
});

module.exports = { createWardSchema, updateWardSchema };
